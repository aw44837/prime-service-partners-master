{#
/**
 * @file
 * Card display for Person content type.
 */
#}
{{ include('dripyard_base:card', {
  title: label,
  image: content.dripyard_image|add_suggestion('bare'),
  eyebrow_text: content.dripyard_position|render|striptags,
  body: content.dripyard_department|render|striptags,
  href: url,
  }, with_context = false) }}
