{#
/**
 * @file
 * Card display for Program content type.
 */
#}
{{ include('dripyard_base:card', {
  title: label,
  image: content.dripyard_image|add_suggestion('bare'),
  eyebrow_text: content.dripyard_department|render|striptags,
  body: content.body|render|striptags|trim|slice(0, 150) ~ '...',
  href: url,
  }, with_context = false) }}
