{#
/**
 * @file
 * Card display for Event content type using event-listing-item component.
 */
#}
{% set event_date = node.dripyard_date_start.value ~ 'Z' %}
{{ include('dripyard_base:event-listing-item', {
  title: label,
  url: url,
  date_time_stamp_begin: event_date,
  description: content.body|render|striptags|trim|slice(0, 150) ~ '...',
  location: content.dripyard_location|render|striptags,
  }, with_context = false) }}
